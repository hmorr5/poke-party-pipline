#  Template NodeJS build

#  This template allows you to validate your NodeJS code.
#  The workflow allows running tests and code linting on the default branch.

image: node:16

pipelines:
  default:
    - parallel:
        - step:
            name: Build
            caches:
              - node
            script:
              - npm install
        - step:
            name: Code linting
            script:
              - npm install eslint
            caches:
              - node
        - step:
            name: Sync GitHub Mirror
            image: alpine/git:latest
            clone:
              enabled: false
            script:
            - git clone --bare git@bitbucket.org:pokemonthing/nodejs-react-app.git
            - cd nodejs-react-app.git
            - git push --mirror git@github.com:hmorr5/poke-party-pipline.git
